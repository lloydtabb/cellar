window.__MODEL_FILES__ = {"file:///cellar.malloy":"// The cellar semantic model.\n//\n// Three grains, and that's the whole thing:\n//\n//   bottles        one row per bottle EVER — on the rack or long gone\n//   purchases      one row per purchase lot (N bottles of one wine, one order)\n//   tasting_notes  one row per note written\n//\n// `bottles` is the union of CellarTracker's inventory.csv and consumed.csv,\n// separated by `status`. Keeping them together is what makes the interesting\n// questions one-liners: what you bought and what you drank sit on a single\n// timeline, and \"6 bought, 4 drunk, 2 left\" is one group-by instead of a stitch\n// across two tables. DuckDB scans the 15k rows fast enough that nothing here\n// needs pre-aggregating.\n//\n// Everything is scoped by `cellar` — three cellars live side by side, and 427\n// wines appear in more than one of them, so nothing joins on wine alone.\n\nimport \"storage.malloy\"\n\nsource: cellars is cellars_table extend {\n  primary_key: cellar\n}\n\nsource: wines is wines_table extend {\n  primary_key: wine_id\n}\n\n// ── tasting notes ───────────────────────────────────────────────────────────\n// Defined first so `bottles` can join it: a bottle's rating is whatever you\n// wrote about that wine.\n\nsource: tasting_notes is notes_table extend {\n  primary_key: note_id\n  join_one: owner is cellars on cellar = owner.cellar\n\n  dimension:\n    style is\n      pick 'Sparkling' when wine_type ~ '%Sparkling%'\n      pick 'Red' when wine_type ~ 'Red%'\n      pick 'White' when wine_type ~ 'White%'\n      pick 'Rosé' when wine_type ~ 'Ros%'\n      else 'Other'\n\n    vintage_label is\n      pick 'NV' when is_non_vintage\n      else vintage::string\n\n    // Notes that share an iEvent are one sitting — a vertical, a book club, a\n    // Champagne Saturday.\n    is_event is event_id is not null\n\n    score_band is\n      pick 'Outstanding (95+)' when rating >= 95\n      pick 'Excellent (93–94)' when rating >= 93\n      pick 'Very good (90–92)' when rating >= 90\n      pick 'Good (87–89)' when rating >= 87\n      else 'Ordinary (<87)'\n\n    // One haystack for the Tasting Notes search box, lowercased once here so\n    // the dashboard doesn't have to. Covers everything someone might half-\n    // remember about a note: the wine, who made it, the night it was opened,\n    // and the note itself — so \"book club\" finds the event and \"forest floor\"\n    // finds the prose, from the same box. coalesce because any of them can be\n    // null, and concat would swallow the whole string if one were.\n    searchable is lower(concat(\n      coalesce(wine, ''), ' ',\n      coalesce(producer, ''), ' ',\n      coalesce(event_title, ''), ' ',\n      coalesce(tasting_note, '')))\n\n  measure:\n    note_count is count()\n    avg_rating is rating.avg()\n    best_rating is rating.max()\n    rated_wine_count is count(wine_id)\n    event_count is count(event_id)\n\n  view: by_producer is {\n    group_by: producer\n    aggregate: note_count, avg_rating, best_rating\n    having: note_count >= 3\n    order_by: avg_rating desc\n    limit: 15\n  }\n\n  view: by_region is {\n    group_by: region\n    aggregate: note_count, avg_rating\n    having: note_count >= 3\n    order_by: avg_rating desc\n    limit: 12\n  }\n\n  view: ratings_over_time is {\n    group_by: tasted is tasting_date.year\n    aggregate: note_count, avg_rating\n    order_by: tasted asc\n  }\n\n  view: score_distribution is {\n    group_by: rating\n    aggregate: note_count\n    where: rating is not null\n    order_by: rating asc\n  }\n\n  view: highest_rated is {\n    group_by: wine, producer, vintage_label, rating, tasting_date\n    order_by: rating desc\n    limit: 25\n  }\n\n  view: recent_notes is {\n    group_by: tasting_date, wine, producer, vintage_label, rating, tasting_note\n    order_by: tasting_date desc\n    limit: 30\n  }\n\n  view: tastings is {\n    group_by: event_title, event_date, event_location\n    aggregate: bottles_opened is note_count, avg_rating\n    where: is_event\n    order_by: event_date desc\n    limit: 40\n  }\n}\n\n// ── every bottle, on the rack or gone ───────────────────────────────────────\n\nsource: bottles is bottles_table extend {\n  primary_key: bottle_key\n  join_one: owner is cellars on cellar = owner.cellar\n  // Fans out — a wine with 6 bottles and 2 notes makes 12 rows. Malloy's\n  // symmetric aggregates keep bottle counts honest through it, so `notes.*`\n  // can be read straight off the bottle grain.\n  join_many: notes is tasting_notes on cellar = notes.cellar and wine_id = notes.wine_id\n\n  dimension:\n    // How you actually go find a bottle: room, then bin.\n    shelf is concat(location, ' · ', coalesce(bin, '—'))\n\n    style is\n      pick 'Sparkling' when wine_type ~ '%Sparkling%'\n      pick 'Red' when wine_type ~ 'Red%'\n      pick 'White' when wine_type ~ 'White%'\n      pick 'Rosé' when wine_type ~ 'Ros%'\n      else 'Other'\n\n    vintage_label is\n      pick 'NV' when is_non_vintage\n      else vintage::string\n\n    // Age of the WINE (from its vintage), not of the purchase.\n    bottle_age is year(now) - vintage\n\n    // Time on the rack: until it was opened, or until today if it's still here.\n    years_held is days(purchase_date to coalesce(exit_date, now::date)) / 365.25\n\n    age_when_opened is exit_date.year - vintage\n\n    price_band is\n      pick 'Under $25' when bottle_price < 25\n      pick '$25–50' when bottle_price < 50\n      pick '$50–100' when bottle_price < 100\n      pick '$100–250' when bottle_price < 250\n      pick '$250–500' when bottle_price < 500\n      else '$500+'\n\n    price_band_rank is\n      pick 1 when bottle_price < 25\n      pick 2 when bottle_price < 50\n      pick 3 when bottle_price < 100\n      pick 4 when bottle_price < 250\n      pick 5 when bottle_price < 500\n      else 6\n\n  measure:\n    // Everything below reads on any slice — a region, a producer, a year.\n    bottle_count is count()\n    on_hand is count() { where: in_cellar }\n    cellar_value is bottle_price.sum() { where: in_cellar }\n    avg_bottle_price is bottle_price.avg() { where: in_cellar }\n    dearest_bottle is bottle_price.max() { where: in_cellar }\n    magnums is count() { where: in_cellar and bottle_size = '1.5L' }\n\n    drunk is count() { where: status = 'Drunk' }\n    value_drunk is bottle_price.sum() { where: status = 'Drunk' }\n    avg_price_drunk is bottle_price.avg() { where: status = 'Drunk' }\n    sold is count() { where: status = 'Sold' }\n    sale_revenue is revenue.sum() { where: status = 'Sold' }\n    lost is count() { where: status = 'Lost' or status = 'Flawed' }\n    given_away is count() { where: status = 'Given away' }\n\n    wine_count is count(wine_id)\n    wines_on_hand is count(wine_id) { where: in_cellar }\n    producer_count is count(producer) { where: in_cellar }\n    region_count is count(region) { where: in_cellar }\n\n    // Reading through the notes join: what you thought of what you own.\n    //\n    // `rating` is BOTTLE-WEIGHTED — the join fans notes across every bottle of\n    // that wine, so a wine you own twelve of counts twelve times. At the wine\n    // grain (every view below that groups by `wine`) that's exactly the wine's\n    // own average. At coarser grains read it as \"the average score behind a\n    // bottle on my rack\", which is the useful reading for a cellar anyway. For\n    // an unweighted average over the notes themselves, use `tasting_notes`.\n    rating is notes.rating.avg()\n    // Distinct — count() here would count fanned-out join rows.\n    note_count is count(notes.note_id)\n\n  // ── what's on the rack ────────────────────────────────────────────────────\n\n  view: by_region is {\n    group_by: region\n    aggregate: on_hand, cellar_value\n    where: in_cellar\n    order_by: on_hand desc\n    limit: 12\n  }\n\n  view: by_style is {\n    group_by: style\n    aggregate: on_hand, cellar_value\n    where: in_cellar\n    order_by: on_hand desc\n  }\n\n  view: by_producer is {\n    group_by: producer\n    aggregate: on_hand, cellar_value, rating\n    where: in_cellar\n    order_by: on_hand desc\n    limit: 15\n  }\n\n  view: by_country is {\n    group_by: country\n    aggregate: on_hand, cellar_value\n    where: in_cellar\n    order_by: on_hand desc\n    limit: 10\n  }\n\n  view: by_vintage is {\n    group_by: vintage\n    aggregate: on_hand\n    where: in_cellar and vintage is not null\n    order_by: vintage asc\n  }\n\n  view: by_price_band is {\n    group_by: price_band, price_band_rank\n    aggregate: on_hand, cellar_value\n    where: in_cellar\n    order_by: price_band_rank asc\n  }\n\n  view: by_location is {\n    group_by: location\n    aggregate: on_hand, cellar_value\n    where: in_cellar\n    order_by: on_hand desc\n  }\n\n  view: by_size is {\n    group_by: bottle_size\n    aggregate: on_hand, cellar_value\n    where: in_cellar\n    order_by: on_hand desc\n  }\n\n  // ── the wine-level view: what's left, what's gone, what you thought ───────\n\n  view: deep_holdings is {\n    group_by: wine, producer, region, vintage_label\n    aggregate: on_hand, drunk, cellar_value, rating\n    having: on_hand > 0\n    order_by: on_hand desc\n    limit: 25\n  }\n\n  // The last bottle of something you loved is the most actionable row in a\n  // cellar — you either open it or you never do.\n  view: last_bottles is {\n    group_by: wine, producer, region, vintage_label\n    aggregate: rating, drunk, value is cellar_value\n    having: on_hand = 1 and rating >= 92\n    order_by: rating desc\n    limit: 25\n  }\n\n  // Bought, shelved, never opened, never written up.\n  view: never_tasted is {\n    group_by: wine, producer, region, vintage_label\n    aggregate: on_hand, cellar_value, held is years_held.avg()\n    having: note_count = 0 and on_hand > 0\n    order_by: held desc\n    limit: 25\n  }\n\n  view: oldest_on_hand is {\n    group_by: wine, producer, region, vintage\n    aggregate: on_hand, cellar_value\n    where: in_cellar and vintage is not null\n    order_by: vintage asc\n    limit: 25\n  }\n\n  // ── flow: in and out over time ────────────────────────────────────────────\n\n  view: drinking_by_year is {\n    group_by: drank is exit_date.year\n    aggregate: drunk, value_drunk, avg_price_drunk\n    where: status = 'Drunk'\n    order_by: drank asc\n  }\n\n  view: drinking_by_month is {\n    group_by: drank is exit_date.month\n    aggregate: drunk, value_drunk\n    where: status = 'Drunk'\n    order_by: drank asc\n  }\n\n  view: by_status is {\n    group_by: status\n    aggregate: bottle_count, cost is bottle_price.sum()\n    where: not in_cellar\n    order_by: bottle_count desc\n  }\n\n  view: drunk_by_region is {\n    group_by: region\n    aggregate: drunk, value_drunk\n    where: status = 'Drunk'\n    order_by: drunk desc\n    limit: 12\n  }\n\n  view: drunk_by_producer is {\n    group_by: producer\n    aggregate: drunk, avg_price_drunk, rating\n    where: status = 'Drunk'\n    order_by: drunk desc\n    limit: 15\n  }\n\n  // Weekday drinking is the everyday cellar; weekend is the occasion cellar.\n  view: by_weekday is {\n    group_by: weekday is day_of_week(exit_date)\n    aggregate: drunk, avg_price_drunk\n    where: status = 'Drunk'\n    order_by: weekday asc\n  }\n\n  view: recent_pours is {\n    group_by: exit_date, wine, producer, vintage_label, bottle_price, occasion\n    where: status = 'Drunk'\n    order_by: exit_date desc\n    limit: 40\n  }\n\n  // How long things sit before they get opened.\n  view: holding_time is {\n    group_by: years_shelved is floor(years_held)\n    aggregate: drunk\n    where: status = 'Drunk'\n    order_by: years_shelved asc\n  }\n}\n\n// ── what you bought ─────────────────────────────────────────────────────────\n// Kept separate from `bottles` because the grain is the LOT, not the bottle:\n// this is where order-level questions live (what shipped together, what's\n// still in transit, what a case cost).\n\nsource: purchases is purchases_table extend {\n  join_one: owner is cellars on cellar = owner.cellar\n\n  dimension:\n    style is\n      pick 'Sparkling' when wine_type ~ '%Sparkling%'\n      pick 'Red' when wine_type ~ 'Red%'\n      pick 'White' when wine_type ~ 'White%'\n      pick 'Rosé' when wine_type ~ 'Ros%'\n      else 'Other'\n\n    vintage_label is\n      pick 'NV' when is_non_vintage\n      else vintage::string\n\n    days_to_delivery is days(purchase_date to delivery_date)\n\n    price_band is\n      pick 'Under $25' when bottle_price < 25\n      pick '$25–50' when bottle_price < 50\n      pick '$50–100' when bottle_price < 100\n      pick '$100–250' when bottle_price < 250\n      pick '$250–500' when bottle_price < 500\n      else '$500+'\n\n  measure:\n    lot_count is count()\n    bottles_bought is quantity.sum()\n    total_spend is lot_cost.sum()\n    avg_bottle_price is bottle_price.avg()\n    dearest_bottle is bottle_price.max()\n    order_count is count(order_number)\n    store_count is count(store_name)\n    wine_count is count(wine_id)\n    undelivered_bottles is quantity.sum() { where: not delivered }\n    undelivered_value is lot_cost.sum() { where: not delivered }\n    avg_days_to_delivery is days_to_delivery.avg()\n\n  view: spend_by_year is {\n    group_by: bought is purchase_date.year\n    aggregate: total_spend, bottles_bought, avg_bottle_price\n    order_by: bought asc\n  }\n\n  view: spend_by_month is {\n    group_by: bought is purchase_date.month\n    aggregate: total_spend, bottles_bought\n    order_by: bought asc\n  }\n\n  view: by_store is {\n    group_by: store_name\n    aggregate: total_spend, bottles_bought, avg_bottle_price, order_count\n    order_by: total_spend desc\n    limit: 15\n  }\n\n  view: by_region is {\n    group_by: region\n    aggregate: total_spend, bottles_bought, avg_bottle_price\n    order_by: total_spend desc\n    limit: 12\n  }\n\n  view: by_style is {\n    group_by: style\n    aggregate: total_spend, bottles_bought\n    order_by: total_spend desc\n  }\n\n  // The taste-drift chart: what share of each year's buying went where.\n  view: region_mix_by_year is {\n    group_by: bought is purchase_date.year\n    aggregate: bottles_bought\n    nest: regions is {\n      group_by: region\n      aggregate: bottles_bought\n      order_by: bottles_bought desc\n      limit: 8\n    }\n    order_by: bought asc\n  }\n\n  // Which calendar months you actually buy in — auction season, release windows,\n  // December gifting.\n  view: seasonality is {\n    group_by: month_of_year is month(purchase_date)\n    aggregate: bottles_bought, total_spend\n    order_by: month_of_year asc\n  }\n\n  view: in_transit is {\n    group_by: purchase_date, wine, producer, store_name, quantity, lot_cost\n    where: not delivered\n    order_by: purchase_date desc\n    limit: 40\n  }\n\n  view: recent_orders is {\n    group_by: purchase_date, store_name, order_number\n    aggregate: wines is wine_count, bottles_bought, total_spend\n    order_by: purchase_date desc\n    limit: 30\n  }\n\n  view: biggest_lots is {\n    group_by: purchase_date, wine, producer, vintage_label, quantity, bottle_price\n    aggregate: cost is total_spend\n    order_by: cost desc\n    limit: 25\n  }\n}\n","file:///dashboards/buying.malloy":"##! experimental { givens }\n\n// Runs on `purchases` (grain = the LOT) rather than `bottles`, because the\n// questions here are order-shaped: what did this shipment cost, what's still in\n// transit, which merchant gets the most of my money.\n\nimport \"../cellar.malloy\"\nimport \"../givens.malloy\"\n\n#\" What you're spending, where you're spending it, and what's still in transit.\n# artifact { title=\"Buying\" }\n# dashboard { columns=6 }\nquery: buying is purchases -> {\n  where:\n    owner.cellar_name ~ $CELLAR,\n    region ~ $REGION,\n    producer ~ $PRODUCER,\n    style ~ $STYLE,\n    purchase_date ~ $PERIOD\n\n  # colspan=2\n  aggregate:\n    # label=\"Total spend\" currency\n    total_spend\n    # label=\"Bottles bought\"\n    bottles_bought\n    # label=\"Average bottle\" currency\n    avg_bottle_price\n    # label=\"Orders\"\n    order_count\n    # label=\"Bottles in transit\"\n    undelivered_bottles\n    # label=\"Value in transit\" currency\n    undelivered_value\n\n  # colspan=3\n  nest:\n    # break\n    # line_chart\n    # label=\"Spend by year\"\n    spend_by_year\n    # bar_chart\n    # label=\"Where you buy\"\n    by_store\n    # bar_chart\n    # label=\"What you buy\"\n    by_region\n    // Auction season, mailing-list release windows, December gifting — buying is\n    // never evenly spread across the year.\n    # bar_chart\n    # label=\"Which months you buy in\"\n    seasonality\n\n  # colspan=6\n  nest:\n    # break\n    # label=\"Still in transit\"\n    in_transit\n    # label=\"Recent orders\"\n    recent_orders\n    # label=\"Biggest single lots\"\n    biggest_lots\n}\n","file:///dashboards/drinking.malloy":"##! experimental { givens }\n\n// The other half of the ledger. Bottles leave a cellar five ways, and only one\n// of them is the good one — `by_status` is where the corked bottles, the ones\n// that went missing in a move, and the consignment sale show up.\n\nimport \"../cellar.malloy\"\nimport \"../givens.malloy\"\n\n#\" What you've been drinking, how fast, and what left the cellar another way.\n# artifact { title=\"Drinking\" }\n# dashboard { columns=6 }\nquery: drinking is bottles -> {\n  where:\n    owner.cellar_name ~ $CELLAR,\n    region ~ $REGION,\n    producer ~ $PRODUCER,\n    style ~ $STYLE,\n    exit_date ~ $PERIOD\n\n  # colspan=2\n  aggregate:\n    # label=\"Bottles drunk\"\n    drunk\n    # label=\"Value drunk\" currency\n    value_drunk\n    # label=\"Average bottle opened\" currency\n    avg_price_drunk\n    # label=\"Sold\"\n    sold\n    # label=\"Sale revenue\" currency\n    sale_revenue\n    # label=\"Lost or flawed\"\n    lost\n\n  # colspan=3\n  nest:\n    # break\n    # line_chart\n    # label=\"Bottles opened by year\"\n    drinking_by_year\n    # bar_chart\n    # label=\"What you drink\"\n    drunk_by_region\n    # bar_chart\n    # label=\"Who you drink\"\n    drunk_by_producer\n    // Weekday drinking is the everyday cellar; the weekend is the occasion one.\n    # bar_chart\n    # label=\"Which day of the week\"\n    by_weekday\n    # bar_chart\n    # label=\"Years held before opening\"\n    holding_time\n    # bar_chart\n    # label=\"How bottles left\"\n    by_status\n\n  # colspan=6\n  nest:\n    # break\n    # label=\"Recently opened\"\n    recent_pours\n}\n","file:///dashboards/open_next.malloy":"##! experimental { givens }\n\n// \"Pick something for me.\"\n//\n// This page used to be three ranked tables — last bottles, never-opened,\n// oldest — which is the same job done as homework. Now the page commits to one\n// bottle and tells you about it, and the tables' logic survives as the moods in\n// open_next.jsx (Last bottle / Never opened / Been waiting).\n//\n// The query's job is to hand the component a POOL, not an answer: every bottle\n// on the rack that passes the filters, one row per wine per shelf, carrying\n// everything the card needs. Choosing from the pool is interaction, so it lives\n// in the component — that way \"pick another\" is instant and doesn't re-query.\n//\n// One row per (wine, location, bin) rather than per wine, because the card\n// tells you which shelf to walk to, and a wine split across two rooms is two\n// different walks.\n\nimport \"../cellar.malloy\"\nimport \"../givens.malloy\"\n\n#\" Can't decide? Let the cellar choose, and tell you why.\n# artifact { title=\"Open Next\" }\nquery: open_next is bottles -> {\n  where:\n    owner.cellar_name ~ $CELLAR,\n    region ~ $REGION,\n    producer ~ $PRODUCER,\n    style ~ $STYLE,\n    // Bottles CellarTracker still marks pending have no shelf yet — they may\n    // not even have arrived. You can't walk to one, so they're not candidates.\n    location != '(pending)'\n    // NOTE: no `in_cellar` here. The measures below carry their own status\n    // filters (`on_hand` counts in-cellar, `already_drunk` counts drunk), so\n    // filtering the stage to in-cellar rows would zero out the drinking history\n    // this card is built to show. `having: on_hand > 0` does the job instead.\n\n  group_by:\n    wine\n    producer\n    region\n    appellation\n    varietal\n    style\n    vintage_label\n\n  aggregate:\n    on_hand\n    stock_value is cellar_value\n    price is avg_bottle_price\n    // Bottle-weighted, but this groups by wine, so it's the wine's own average.\n    score is rating\n    notes_written is note_count\n    already_drunk is drunk\n    years_on_rack is years_held.avg() { where: in_cellar }\n\n  // Only wines you can actually pull a bottle of tonight.\n  having: on_hand > 0\n\n  // Where to walk. A wine split across two rooms lists both.\n  nest: shelves is {\n    group_by: location, bin, bottle_size\n    aggregate: bottles_here is on_hand\n    where: in_cellar\n    order_by: bottles_here desc\n    // Bottles scatter one-per-bin, so a wine you own a dozen of can occupy a\n    // dozen shelves. 12 covers nearly everything; the component adds an\n    // \"+N elsewhere\" chip when the listed shelves don't account for on_hand.\n    limit: 12\n  }\n\n  // The last thing you said about it, to print on the card. Three so the\n  // component can fall back if the most recent note has no text.\n  nest: your_notes is {\n    // Aliased (not `notes.rating`) because order_by takes output names, and\n    // because a bare `rating` would collide with the bottles measure of that name.\n    group_by:\n      note_date is notes.tasting_date\n      note_score is notes.rating\n      note_text is notes.tasting_note\n    order_by: note_date desc\n    limit: 3\n  }\n\n  order_by: on_hand desc\n  limit: 400\n}\n","file:///dashboards/overview.malloy":"##! experimental { givens }\n\n// The landing dashboard: what's on the rack right now.\n//\n// Every tile is scoped to bottles still in the cellar (the `on_hand` /\n// `cellar_value` measures carry `where: in_cellar` themselves), so this reads as\n// a snapshot rather than a history. The history lives in buying/ and drinking/.\n\nimport \"../cellar.malloy\"\nimport \"../givens.malloy\"\n\n#\" Everything on the rack right now — how much, from where, and what it's worth.\n# artifact { title=\"The Cellar\" }\n# dashboard { columns=6 }\nquery: overview is bottles -> {\n  where:\n    owner.cellar_name ~ $CELLAR,\n    region ~ $REGION,\n    producer ~ $PRODUCER,\n    style ~ $STYLE\n\n  # colspan=2\n  aggregate:\n    # label=\"Bottles on hand\"\n    on_hand\n    # label=\"Cellar value\" currency\n    cellar_value\n    # label=\"Distinct wines\"\n    wines_on_hand\n    # label=\"Producers\"\n    producer_count\n    # label=\"Average bottle\" currency\n    avg_bottle_price\n    # label=\"Dearest bottle\" currency\n    dearest_bottle\n\n  # colspan=3\n  nest:\n    # break\n    # bar_chart\n    # label=\"Where it comes from\"\n    by_region\n    # bar_chart\n    # label=\"What it costs\"\n    by_price_band\n    # bar_chart\n    # label=\"Red, white, sparkling\"\n    by_style\n    # bar_chart\n    # label=\"Where it's stored\"\n    by_location\n    # line_chart\n    # label=\"Bottles by vintage\"\n    by_vintage\n    # bar_chart\n    # label=\"Bottle sizes\"\n    by_size\n    # colspan=6\n    # label=\"Deepest holdings\"\n    deep_holdings\n}\n","file:///dashboards/tasting.malloy":"##! experimental { givens }\n\n// A search page, not an analysis page.\n//\n// This used to carry a score histogram, an average-by-year line, best-producers\n// and best-regions bars, plus three overlapping tables — which mostly restated\n// each other, and none of which helped anyone find a particular note. What an\n// owner wants from a pile of tasting notes is to find the one they're thinking\n// of, so the whole page is now filters plus one list.\n//\n// Runs on `tasting_notes` (grain = the note), so the averages are unweighted —\n// one note, one vote.\n//\n// The searches compose: producer \"Rousseau\" + note contains \"still closed\"\n// answers \"have I ever written that a Rousseau needed more time\".\n\nimport \"../cellar.malloy\"\nimport \"../givens.malloy\"\n\n#\" Find a note — by wine, producer, region, score, or what you wrote.\n# artifact { title=\"Tasting Notes\" }\n# dashboard { columns=6 }\nquery: tasting is tasting_notes -> {\n  where:\n    owner.cellar_name ~ $CELLAR,\n    wine ~ $WINE,\n    producer ~ $PRODUCER,\n    region ~ $REGION,\n    style ~ $STYLE,\n    rating ~ $SCORE,\n    tasting_date ~ $PERIOD,\n    // `searchable` (see cellar.malloy) is the wine, producer, event title and\n    // note text in one lowercased string. Wrapped in wildcards so a bare word\n    // is a substring search; empty matches everything.\n    searchable ~ concat('%', lower($SEARCH), '%')\n\n  // Just enough to say how big the result set is and whether the slice you've\n  // filtered down to actually scored well.\n  # colspan=2\n  aggregate:\n    # label=\"Notes found\"\n    note_count\n    # label=\"Average score\"\n    avg_rating\n    # label=\"Best score\"\n    best_rating\n\n  # colspan=6\n  nest:\n    # break\n    # label=\"Notes\"\n    matching_notes is {\n      group_by:\n        # label=\"Tasted\"\n        tasting_date\n        # label=\"Score\"\n        rating\n        wine\n        producer\n        # label=\"Vintage\"\n        vintage_label\n        # label=\"Opened at\"\n        event_title\n        # label=\"What you wrote\"\n        tasting_note\n      order_by: tasting_date desc\n      limit: 500\n    }\n}\n","file:///givens.malloy":"// The dashboard filter controls.\n//\n// Declared here rather than in cellar.malloy so the model stays given-free and\n// reusable; each dashboard decides for itself which of these it applies.\n//\n// Three cellars share this site, so CELLAR is the one given that is never empty:\n// a cellar overview that blends three people's racks isn't an overview of\n// anything. It filters on the display NAME (via the `owner` join) so the\n// dropdown reads \"The Auction Veteran\" rather than `veteran`.\n\n##! experimental { givens }\n\nimport \"cellar.malloy\"\n\ngiven:\n  # label=\"Cellar\" control=select suggest { query=cellar_suggest dimension=cellar_name }\n  CELLAR :: filter<string> is f'The Auction Veteran'\n\n  # label=\"Region\" suggest { query=region_suggest dimension=region }\n  REGION :: filter<string> is f''\n\n  # label=\"Producer\" suggest { query=producer_suggest dimension=producer }\n  PRODUCER :: filter<string> is f''\n\n  # label=\"Style\" control=multiselect suggest { query=style_suggest dimension=style }\n  STYLE :: filter<string> is f''\n\n  // Applied to whichever date the dashboard is about — bought, drunk, tasted.\n  # label=\"Time period\"\n  PERIOD :: filter<date> is f''\n\n  # label=\"Wine\" suggest { query=wine_suggest dimension=wine }\n  WINE :: filter<string> is f''\n\n  # label=\"Score\" range_min=75 range_max=100\n  SCORE :: filter<number> is f''\n\n  // Free text over the note itself — \"forest floor\", \"corked\", \"with Sam\".\n  // A plain string rather than a filter<string> so the box can be typed into\n  // literally: the query wraps it in wildcards and lowercases both sides, so\n  // \"Forest Floor\" and \"forest\" both find the same notes. Empty matches\n  // everything, which is the natural \"no search\".\n  # label=\"Note contains\"\n  SEARCH :: string is ''\n\n// Suggestion sources. Each one narrows by the OTHER givens — pick a cellar and\n// the region list becomes that cellar's regions — so the lists never offer a\n// combination that returns nothing. Each query omits its own given, which is\n// what keeps the list from collapsing to the current pick.\n\nquery: cellar_suggest is cellars -> {\n  group_by: cellar_name\n  order_by: cellar_name asc\n}\n\nquery: region_suggest is bottles -> {\n  group_by: region\n  aggregate: bottles_here is on_hand\n  where:\n    owner.cellar_name ~ $CELLAR,\n    producer ~ $PRODUCER,\n    style ~ $STYLE\n  order_by: bottles_here desc\n  limit: 200\n}\n\nquery: producer_suggest is bottles -> {\n  group_by: producer\n  aggregate: bottles_here is on_hand\n  where:\n    owner.cellar_name ~ $CELLAR,\n    region ~ $REGION,\n    style ~ $STYLE\n  order_by: bottles_here desc\n  limit: 500\n}\n\n// Wines you have actually written about — this feeds the Tasting Notes search,\n// so suggesting from the note grain keeps it to wines with something to read.\nquery: wine_suggest is tasting_notes -> {\n  group_by: wine\n  aggregate: notes_here is note_count\n  where:\n    owner.cellar_name ~ $CELLAR,\n    region ~ $REGION,\n    producer ~ $PRODUCER\n  order_by: notes_here desc\n  limit: 500\n}\n\nquery: style_suggest is bottles -> {\n  group_by: style\n  aggregate: bottles_here is on_hand\n  where:\n    owner.cellar_name ~ $CELLAR,\n    region ~ $REGION,\n    producer ~ $PRODUCER\n  order_by: bottles_here desc\n}\n","file:///index.malloy":"// The published query surface — what an MCP client or the claude.ai explore\n// preview is allowed to ask about. Dashboards do NOT come through here; they're\n// discovered as dashboards/*.malloy.\n\nimport \"cellar.malloy\"\n\nexport { bottles, purchases, tasting_notes, wines, cellars }\n","file:///storage.malloy":"// Where the data lives.\n//\n// The parquet sits in docs/data/ — docs/ is the directory GitHub Pages\n// publishes, so one spelling works in every context:\n//\n//   VSCode / malloyyo dashboard dev   DuckDB reads docs/data/*.parquet off disk\n//   malloyyo dashboard bundle         the site IS docs/, so the browser fetches\n//                                     ./data/*.parquet and registers it under\n//                                     this same name\n//\n// Rebuild these from the CSV exports with `python3 scripts/build_data.py`.\n\nsource: cellars_table is duckdb.table('docs/data/cellars.parquet') extend {}\nsource: wines_table is duckdb.table('docs/data/wines.parquet') extend {}\nsource: bottles_table is duckdb.table('docs/data/bottles.parquet') extend {}\nsource: purchases_table is duckdb.table('docs/data/purchases.parquet') extend {}\nsource: notes_table is duckdb.table('docs/data/notes.parquet') extend {}\n"};
window.__TABLE_FILES__ = {"docs/data/bottles.parquet":"./data/bottles.parquet","docs/data/cellars.parquet":"./data/cellars.parquet","docs/data/notes.parquet":"./data/notes.parquet","docs/data/purchases.parquet":"./data/purchases.parquet","docs/data/wines.parquet":"./data/wines.parquet"};
